[project]
name = "grabnotate"
version = "0.1.0"
description = "A Wayland screenshot and annotation tool for GNOME"
requires-python = ">=3.14"
dependencies = [
    "pycairo>=1.29.0",
    "pygobject>=3.56.2",
]

[project.scripts]
grabnotate = "screenshot_tool.main:main"
